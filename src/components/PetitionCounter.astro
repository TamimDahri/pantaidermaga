---
import { supabase } from "../lib/supabase";

const { data } = await supabase.rpc("get_petition_count");
const count = data ?? 0;
---

<div class="petition-counter">
  <div class="counter-icon">✍️</div>

  <span class="counter-number" data-petition-count>
    ...
  </span>

  <span class="counter-label">
    rakyat telah menyokong petisyen ini
  </span>
</div>

<script>
  import "../scripts/petition-counter";
</script>